spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/mp?serverTimezong=UTC
    password: Bingoing
    username: root


mybatis-plus:
  global-config:
    db-config:
      table-prefix: mp_

