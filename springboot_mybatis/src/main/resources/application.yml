#datasource
spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/mp
    username: root
    password: Bingoing
    driver-class-name: com.mysql.cj.jdbc.Driver

#MyBatis
mybatis:
  mapper-locations: classpath:mapper/*Mapper.xml # mapper映射文件路径
  type-aliases-package: com.bingo.learn.domain
#  config-location:  # 用于配置mybatis核心配置文件